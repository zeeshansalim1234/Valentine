# Checkpoint photos

Drop your checkpoint images here (e.g. `first-date.jpg`, `beach-day.jpg`).

In `main.js`, set each checkpoint’s `imageSrc` to point to a file in this folder:

```js
imageSrc: "./photos/your-photo.jpg",
```

Use common image formats: `.jpg`, `.jpeg`, `.png`, `.webp`.
